import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["example.com", "res.cloudinary.com", "via.placeholder.com"], // Add other image domains as needed
  },
};

export default nextConfig;
